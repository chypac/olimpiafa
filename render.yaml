services:
  - type: web
    name: quiz-app
    env: python
    buildCommand: cd frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt
    startCommand: cd backend && gunicorn app_unified:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
